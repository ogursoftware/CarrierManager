<p style="color: green"><%= notice %></p>

<%= render @load %>
<%= link_to "Download Rate Confirmation", @load.rate_confirmation.url, download: true %>
<div>
  <p>
  <%= link_to "Edit this Load", edit_load_path(@load), class:"btn btn-dark" %> |  
  <%= link_to "Back to Loads", loads_path , class:"btn btn-dark" %>
  <p>
  <%= button_to "Delete this Load", @load, method: :delete , class:"btn btn-danger" %>
</div>
